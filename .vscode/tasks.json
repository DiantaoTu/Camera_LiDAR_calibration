{
    "tasks": [
        {
            "type": "shell",
            "label": "cmake",
            "command": "cmake -DCMAKE_BUILD_TYPE=Debug .. && make -j4",
            "args": [
            
            ],
            "options": {
                "cwd": "${workspaceFolder}/build"
            }
        }
    ],
    "version": "2.0.0"
}